{% liquid
  assign is_empty = true
  for block in section.blocks
    if block.settings.title != blank
      assign is_empty = false
    endif
  endfor

  if section.settings.collections != blank
    assign is_empty = false
  endif

  assign hide_because_empty = false
  if request.design_mode == false and section.settings.hide_if_empty and is_empty
    assign hide_because_empty = true
  endif
%}
{%- unless hide_because_empty -%}
  {{ 'section-filters.css' | asset_url | stylesheet_tag }}
  {{ 'component-tooltip.css' | asset_url | stylesheet_tag }}

  <script src="{{ 'section-filters.js' | asset_url }}" defer="defer"></script>

  {% liquid
    assign color_scheme_string = section.settings.color_scheme | append: ''
    assign section_content_class = 'section-content'
    unless color_scheme_string == 'background-1' or section.settings.height == 'custom'
      assign section_content_class = section_content_class | append: ' section-content-padded'
    endunless
  %}

  <style>
    {% if section.settings.height == 'custom' %}
      #shopify-section-{{ section.id }} .section-content {
        padding-top: 4rem;
        padding-bottom: 4rem;
      }

      @media (min-width: 990px) {
        #shopify-section-{{ section.id }} .section-content {
          height: {{ section.settings.height_custom }}px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          padding-top: 0;
          padding-bottom: 0;
        }
      }
    {% endif %}

    {% if section.settings.background_image %}
      {% if section.settings.full_width %}
        #shopify-section-{{ section.id }} .section-filters-wrapper {
          background-image: url({{ section.settings.background_image | image_url }});
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
          position: relative;
        }

        {% if section.settings.overlay_color and section.settings.overlay_color != blank %}
          #shopify-section-{{ section.id }} .section-filters-wrapper::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: {{ section.settings.overlay_color }};
            pointer-events: none;
          }

          #shopify-section-{{ section.id }} .container {
            position: relative;
            z-index: 1;
          }
        {% endif %}
      {% else %}
        #shopify-section-{{ section.id }} .section-content {
          background-image: url({{ section.settings.background_image | image_url }});
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
          position: relative;
        }

        {% if section.settings.overlay_color and section.settings.overlay_color != blank %}
          #shopify-section-{{ section.id }} .section-content::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: {{ section.settings.overlay_color }};
            pointer-events: none;
          }

          #shopify-section-{{ section.id }} .section-content > * {
            position: relative;
            z-index: 1;
          }
        {% endif %}
      {% endif %}
    {% endif %}

    #shopify-section-{{ section.id }} .section-content {
      margin-top: {{ section.settings.spacing_top_mobile }}px;
      margin-bottom: {{ section.settings.spacing_bottom_mobile }}px;
    }

    #shopify-section-{{ section.id }} .section-content-padded {
      margin-top: 0;
      margin-bottom: 0;
      padding-top: {{ section.settings.spacing_top_mobile }}px;
      padding-bottom: {{ section.settings.spacing_bottom_mobile }}px;
    }

    @media (min-width: 990px) {
      #shopify-section-{{ section.id }} .section-content {
        margin-top: {{ section.settings.spacing_top }}px;
        margin-bottom: {{ section.settings.spacing_bottom }}px;
      }

      #shopify-section-{{ section.id }} .section-content-padded {
        margin-top: 0;
        margin-bottom: 0;
        padding-top: {{ section.settings.spacing_top }}px;
        padding-bottom: {{ section.settings.spacing_bottom }}px;
      }
    }
  </style>

  {%- capture visibility_classes -%}{%- render 'section-visibility-classes' -%}{%- endcapture -%}
  {%- assign visibility_classes = visibility_classes | strip -%}

  <div class="{{ visibility_classes }}">
    <div class="section-filters-wrapper {% if section.settings.full_width %}section-filters-wrapper-fullwidth color-{{ section.settings.color_scheme }}{% endif %}">
      <div class="container">
        <div class="{{ section_content_class }} {% if section.settings.full_width == false %}color-{{ section.settings.color_scheme }}{% endif %}">
          {% if section.settings.heading != blank or section.settings.subheading != blank %}
            <div class="section-headings {% if section.settings.subheading != blank %}with-subheading{% endif %}">
              <div class="section-headings-content{% if section.settings.headings_alignment == 'center' %} center-aligned{% endif %}">
                {% if section.settings.heading != blank %}
                  <{{ section.settings.heading_tag }} class="section-heading heading-size-{{ section.settings.heading_size }}">
                    {{- section.settings.heading -}}
                  </{{ section.settings.heading_tag }}>
                {% endif %}
                {% if section.settings.subheading != blank %}
                  <p class="section-subheading">{{ section.settings.subheading }}</p>
                {% endif %}
              </div>
            </div>
          {% endif %}

          <div class="section-filters">
            <div class="section-filters-form-wrap {% if section.blocks.size > 4 %}section-filters-form-wrap-long{% endif %}">
              <collections-filtering>
                <form action="" method="get" class="section-filters-form">
                  {% if section.settings.collections.count > 1 %}
                    <div class="form-group">
                      {% if section.settings.collections_tooltip != empty %}
                        <div class="label-with-tooltip">
                      {% endif %}
                      <label for="{{ section.id }}-collection">
                        {{ section.settings.collections_label }}
                      </label>
                      {% if section.settings.collections_tooltip != empty %}
                        <button
                          type="button"
                          class="tooltip"
                          aria-label="{{ section.settings.collections_tooltip }}"
                        >
                          <span>{{ section.settings.collections_tooltip }}</span>
                        </button>
                      {% endif %}
                      {% if section.settings.collections_tooltip != empty %}
                        </div>
                      {% endif %}

                      <select
                        class="dropdown-collection"
                        id="{{ section.id }}-collection"
                        data-index="0"
                      >
                        <option class="default" value="" selected hidden>
                          {{ section.settings.collections_label }}
                        </option>
                        {% for collection in section.settings.collections %}
                          <option value="{{ collection.url }}">{{ collection.title }}</option>
                        {% endfor %}
                        {% for collection in section.settings.collections_2 %}
                          <option value="{{ collection.url }}">{{ collection.title }}</option>
                        {% endfor %}
                      </select>
                    </div>
                  {% endif %}

                  {% if section.settings.collections.count == 1 %}
                    {% assign collection = section.settings.collections | first %}
                    <input
                      class="input-collection"
                      aria-label="{{ collection.title }}"
                      type="text"
                      hidden
                      value="{{ collection.url }}"
                    >
                  {% endif %}

                  {% for block in section.blocks %}
                    {% liquid
                      assign start_disabled = true
                      if section.settings.collections.count < 2 and forloop.index == 1
                        assign start_disabled = false
                      endif
                    %}
                    <div class="form-group {% if start_disabled %}disabled{% endif %}">
                      {% if block.settings.tooltip != empty %}
                        <div class="label-with-tooltip">
                      {% endif %}
                      <label for="{{ section.id }}-filter-{{ forloop.index }}">
                        {{ block.settings.placeholder }}
                      </label>
                      {% if block.settings.tooltip != empty %}
                        <button
                          type="button"
                          class="tooltip"
                          aria-label="{{ block.settings.tooltip }}"
                        >
                          <span>{{ block.settings.tooltip }}</span>
                        </button>
                      {% endif %}
                      {% if block.settings.tooltip != empty %}
                        </div>
                      {% endif %}

                      <select
                        class="dropdown-filter"
                        id="{{ section.id }}-filter-{{ forloop.index }}"
                        data-label="{{ block.settings.title }}"
                        data-index="{{ forloop.index }}"
                        {% if start_disabled %}
                          disabled
                        {% endif %}
                      >
                        <option class="default" value="" selected disabled>
                          {{ block.settings.placeholder }}
                        </option>
                      </select>
                    </div>
                  {% endfor %}

                  <button type="submit" disabled class="button button-block">
                    {{ 'sections.filters.search' | t }}
                    <div class="button-overlay-spinner hidden">
                      <span class="spinner-xs"></span>
                    </div>
                  </button>
                </form>
              </collections-filtering>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
{%- endunless -%}
{% schema %}
  {
    "name": "t:sections.filters.name",
    "tag": "section",
    "class": "section-filters",
    "disabled_on": {
      "groups": ["custom.overlays"]
    },
    "settings": [
      {
        "type": "header",
        "content": "t:sections.all.section_content.heading"
      },
      {
        "type": "inline_richtext",
        "id": "heading",
        "label": "t:sections.filters.settings.heading.label"
      },
      {
        "type": "select",
        "id": "heading_size",
        "label": "t:sections.all.heading_size.label",
        "default": "md",
        "options": [
          {
            "value": "sm",
            "label": "t:sections.all.heading_size.options.label_sm"
          },
          {
            "value": "md",
            "label": "t:sections.all.heading_size.options.label_md"
          },
          {
            "value": "lg",
            "label": "t:sections.all.heading_size.options.label_lg"
          },
          {
            "value": "xl",
            "label": "t:sections.all.heading_size.options.label_xl"
          },
          {
            "value": "xxl",
            "label": "t:sections.all.heading_size.options.label_xxl"
          }
        ]
      },
      {
        "type": "inline_richtext",
        "id": "subheading",
        "label": "t:sections.filters.settings.subheading.label"
      },
      {
        "type": "select",
        "id": "headings_alignment",
        "label": "t:sections.all.headings_alignment.label",
        "default": "left",
        "options": [
          {
            "value": "left",
            "label": "t:sections.all.headings_alignment.options.label_left"
          },
          {
            "value": "center",
            "label": "t:sections.all.headings_alignment.options.label_center"
          }
        ]
      },
      {
        "type": "collection_list",
        "id": "collections",
        "label": "t:sections.filters.settings.collections.label",
        "info": "t:sections.filters.settings.collections.info"
      },
      {
        "type": "collection_list",
        "id": "collections_2",
        "label": "t:sections.filters.settings.collections_2.label",
        "info": "t:sections.filters.settings.collections_2.info"
      },
      {
        "type": "text",
        "id": "collections_label",
        "label": "t:sections.filters.settings.collections_label.label",
        "default": "Select...",
        "visible_if": "{{ section.settings.collections }}"
      },
      {
        "type": "text",
        "id": "collections_tooltip",
        "label": "t:sections.filters.settings.collections_tooltip.label",
        "visible_if": "{{ section.settings.collections }}"
      },
      {
        "type": "header",
        "content": "t:sections.all.layout.heading"
      },
      {
        "type": "checkbox",
        "id": "full_width",
        "label": "t:sections.filters.settings.full_width.label"
      },
      {
        "type": "select",
        "id": "height",
        "label": "t:sections.filters.settings.height.label",
        "options": [
          {
            "value": "auto",
            "label": "t:sections.filters.settings.height.options.label_auto"
          },
          {
            "value": "custom",
            "label": "t:sections.filters.settings.height.options.label_custom"
          }
        ]
      },
      {
        "type": "range",
        "id": "height_custom",
        "label": "t:sections.filters.settings.height_custom.label",
        "min": 80,
        "max": 500,
        "step": 10,
        "default": 100,
        "visible_if": "{{ section.settings.height == 'custom' }}"
      },

      {
        "type": "header",
        "content": "t:sections.filters.settings.header_1.content"
      },
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "t:sections.all.colors.color_scheme.label",
        "default": "background-1"
      },
      {
        "type": "image_picker",
        "id": "background_image",
        "label": "t:sections.filters.settings.background_image.label"
      },
      {
        "type": "color",
        "id": "overlay_color",
        "label": "t:sections.filters.settings.overlay_color.label",
        "alpha": true
      },
      {
        "type": "header",
        "content": "t:sections.all.section_spacing.heading"
      },
      {
        "type": "select",
        "id": "spacing_mode",
        "label": "t:sections.all.section_spacing.spacing_mode.label",
        "default": "desktop",
        "options": [
          {
            "value": "desktop",
            "label": "t:sections.all.section_spacing.spacing_mode.options.desktop"
          },
          {
            "value": "mobile",
            "label": "t:sections.all.section_spacing.spacing_mode.options.mobile"
          }
        ]
      },
      {
        "type": "range",
        "id": "spacing_top",
        "min": 0,
        "max": 150,
        "step": 2,
        "default": 60,
        "label": "t:sections.all.section_spacing.spacing_top.label",
        "visible_if": "{{ section.settings.spacing_mode == 'desktop' }}"
      },
      {
        "type": "range",
        "id": "spacing_bottom",
        "min": 0,
        "max": 150,
        "step": 2,
        "default": 60,
        "label": "t:sections.all.section_spacing.spacing_bottom.label",
        "visible_if": "{{ section.settings.spacing_mode == 'desktop' }}"
      },
      {
        "type": "range",
        "id": "spacing_top_mobile",
        "min": 0,
        "max": 150,
        "step": 2,
        "default": 48,
        "label": "t:sections.all.section_spacing.spacing_top.label",
        "visible_if": "{{ section.settings.spacing_mode == 'mobile' }}"
      },
      {
        "type": "range",
        "id": "spacing_bottom_mobile",
        "min": 0,
        "max": 150,
        "step": 2,
        "default": 48,
        "label": "t:sections.all.section_spacing.spacing_bottom.label",
        "visible_if": "{{ section.settings.spacing_mode == 'mobile' }}"
      },
      {
        "type": "header",
        "content": "t:sections.all.advanced.seo_heading.label"
      },
      {
        "type": "select",
        "id": "heading_tag",
        "label": "t:sections.all.advanced.heading_tag.label",
        "default": "h2",
        "options": [
          {
            "value": "h1",
            "label": "H1"
          },
          {
            "value": "h2",
            "label": "H2"
          },
          {
            "value": "h3",
            "label": "H3"
          }
        ]
      },
      {
        "type": "header",
        "content": "t:sections.all.advanced.visibility_heading.label"
      },
      {
        "type": "checkbox",
        "id": "show_on_desktop",
        "default": true,
        "label": "t:sections.all.advanced.show_on_desktop.label"
      },
      {
        "type": "checkbox",
        "id": "show_on_mobile",
        "default": true,
        "label": "t:sections.all.advanced.show_on_mobile.label"
      },
      {
        "type": "checkbox",
        "id": "hide_if_empty",
        "default": false,
        "label": "t:sections.filters.settings.hide_if_empty.label",
        "info": "t:sections.all.advanced.hide_if_empty.info"
      }
    ],
    "blocks": [
      {
        "type": "filter",
        "name": "t:sections.filters.settings.blocks.filter.name",
        "settings": [
          {
            "type": "text",
            "id": "title",
            "label": "t:sections.filters.settings.blocks.filter.settings.title.label",
            "info": "t:sections.filters.settings.blocks.filter.settings.title.info"
          },
          {
            "type": "text",
            "id": "placeholder",
            "label": "t:sections.filters.settings.blocks.filter.settings.placeholder.label"
          },
          {
            "type": "text",
            "id": "tooltip",
            "label": "t:sections.filters.settings.blocks.filter.settings.tooltip.label"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "t:sections.filters.presets.name",
        "category": "t:sections.all.preset_category.navigation",
        "blocks": [
          {
            "type": "filter",
            "settings": {
              "title": "Brand",
              "placeholder": "Choose a brand..."
            }
          },
          {
            "type": "filter",
            "settings": {
              "title": "Availability",
              "placeholder": "Choose an availability..."
            }
          }
        ]
      }
    ]
  }
{% endschema %}
