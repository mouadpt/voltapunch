{% liquid
  assign is_empty = true
  if section.settings.products != blank or section.settings.collection.products != blank
    assign is_empty = false
  endif

  assign hide_because_empty = false
  if request.design_mode == false and section.settings.hide_placeholders and is_empty
    assign hide_because_empty = true
  endif
%}
{%- unless hide_because_empty -%}
  {{ 'component-card-overlay.css' | asset_url | stylesheet_tag }}
  {{ 'component-card.css' | asset_url | stylesheet_tag }}
  {{ 'component-price.css' | asset_url | stylesheet_tag }}
  {{ 'component-pills.css' | asset_url | stylesheet_tag }}
  {{ 'component-scroller.css' | asset_url | stylesheet_tag }}
  {{ 'component-rating.css' | asset_url | stylesheet_tag }}

  {%- if section.settings.layout == 'slider' -%}
    {{ 'flickity.css' | asset_url | stylesheet_tag }}
    <script src="{{ 'flickity.pkgd.js' | asset_url }}" defer="defer"></script>
    <script src="{{ 'carousel-slider.js' | asset_url }}" defer="defer"></script>
  {%- endif -%}

  {% liquid
    assign columns = section.settings.columns | at_most: 5
    assign layout_classes = 'grid-3-col-tablet'

    if columns >= 5
      assign layout_classes = layout_classes | append: ' grid-compact grid-5-col-desktop'
    endif

    if columns == 4
      assign layout_classes = layout_classes | append: ' grid-4-col-desktop'
    endif

    if section.settings.layout == 'slider'
      assign layout_classes = layout_classes | append: ' grid-carousel'
    endif

    assign mobile_two_columns = false
    if section.settings.mobile_columns == '2'
      assign layout_classes = layout_classes | prepend: 'product-grid grid grid-2-col '
      assign mobile_two_columns = true
    else
      assign layout_classes = layout_classes | prepend: 'grid grid-1-col '
    endif

    assign color_scheme_string = section.settings.color_scheme | append: ''
    assign section_content_class = 'section-content'
    unless color_scheme_string == 'background-1'
      assign section_content_class = section_content_class | append: ' section-content-padded'
    endunless
  %}

  <style>
    #shopify-section-{{ section.id }} .card-overlay::after {
      {% if section.settings.collection_overlay_type == 'color' %}
        background: {{ section.settings.collection_overlay_color }};
      {% elsif section.settings.collection_overlay_background != blank %}
        background: {{ section.settings.collection_overlay_background }};
      {% endif %}
    }

    #shopify-section-{{ section.id }} .card-overlay .card-overlay-info {
      color: {{ section.settings.collection_text_color }};
    }

    #shopify-section-{{ section.id }} .section-content {
      margin-top: {{ section.settings.spacing_top_mobile }}px;
      margin-bottom: {{ section.settings.spacing_bottom_mobile }}px;
    }

    #shopify-section-{{ section.id }} .section-content-padded {
      margin-top: 0;
      margin-bottom: 0;
      padding-top: {{ section.settings.spacing_top_mobile }}px;
      padding-bottom: {{ section.settings.spacing_bottom_mobile }}px;
    }

    @media (min-width: 990px) {
      #shopify-section-{{ section.id }} .section-content {
        margin-top: {{ section.settings.spacing_top }}px;
        margin-bottom: {{ section.settings.spacing_bottom }}px;
      }

      #shopify-section-{{ section.id }} .section-content-padded {
        margin-top: 0;
        margin-bottom: 0;
        padding-top: {{ section.settings.spacing_top }}px;
        padding-bottom: {{ section.settings.spacing_bottom }}px;
      }
    }
  </style>

  {%- capture visibility_classes -%}{%- render 'section-visibility-classes' -%}{%- endcapture -%}
  {%- assign visibility_classes = visibility_classes | strip -%}

  <div class="{{ visibility_classes }}">
    <div class="color-{{ section.settings.color_scheme }}">
      <div class="container">
        <div class="{{ section_content_class }}">
          {% if section.settings.heading != blank
            or section.settings.subheading != blank
            or section.settings.layout == 'slider'
          %}
            <div class="section-headings {% if section.settings.subheading != blank %}with-subheading{% endif %}">
              {% if section.settings.heading != blank or section.settings.subheading != blank %}
                <div class="section-headings-content{% if section.settings.headings_alignment == 'center' %} center-aligned{% endif %}">
                  {% if section.settings.heading != blank %}
                    <{{ section.settings.heading_tag }} class="section-heading heading-size-{{ section.settings.heading_size }}">
                      {{ section.settings.heading }}
                    </{{ section.settings.heading_tag }}>
                  {% endif %}
                  {% if section.settings.subheading != blank %}
                    <p class="section-subheading">{{ section.settings.subheading }}</p>
                  {% endif %}
                </div>
              {% endif %}

              {% if section.settings.layout == 'slider'
                and section.settings.headings_alignment == 'left'
              %}
                <carousel-navigation
                  for="Carousel-{{ section.id }}"
                  class="section-headings-actions visible-tablet-up"
                >
                  <div class="pill-nav pill-nav-slider">
                    <button
                      class="pill-nav-item pill-nav-item-disabled focus-none js-prev"
                      aria-disabled="true"
                    >
                      <span class="visually-hidden">
                        {{- 'sections.slideshow.previous_slide' | t -}}
                      </span>
                      {% render 'angle', direction: 'left' %}
                    </button>
                    <button class="pill-nav-item js-next" aria-disabled="false">
                      <span class="visually-hidden">{{ 'sections.slideshow.next_slide' | t }}</span>
                      {% render 'angle', direction: 'right' %}
                    </button>
                  </div>
                </carousel-navigation>
              {% endif %}
            </div>
          {% endif %}

          <div class="collection">
            {%- if section.settings.layout == 'slider' -%}
              <carousel-slider id="Carousel-{{ section.id }}">
            {%- endif -%}

            {%- unless section.settings.stack_on_mobile -%}
              <div class="scroller">
                <div class="scroller-inner">
            {%- endunless -%}

            <div class="{{ layout_classes }}">
              {% if section.settings.show_collection == true
                and section.settings.collection_position == 'start'
              %}
                <div class="grid-item">
                  {% render 'card-collection-custom',
                    collection: section.settings.collection,
                    class: 'color-background-1',
                    image: section.settings.collection_image,
                    title: section.settings.collection_title,
                    description: section.settings.collection_description,
                    button_text: section.settings.collection_button_text,
                    settings: section.settings
                  %}
                </div>
              {% endif %}

              {% for product in section.settings.products %}
                {% liquid
                  assign lazyload = false
                  if forloop.index > 2
                    assign lazyload = true
                  endif
                %}
                <div class="grid-item">
                  {% render 'card-product',
                    card_product: product,
                    show_vendor: section.settings.show_vendor,
                    show_stock: section.settings.show_stock,
                    show_rating: section.settings.show_rating,
                    show_sku: section.settings.show_sku,
                    show_add_to_cart: settings.products_show_add_to_cart,
                    mobile_two_columns: mobile_two_columns,
                    event_context: 'FeaturedCollectionSection',
                    lazyload: lazyload
                  %}
                </div>
              {% else %}
                {% for product in section.settings.collection.products
                  limit: section.settings.products_to_show
                %}
                  {% liquid
                    assign lazyload = false
                    if forloop.index > 2
                      assign lazyload = true
                    endif
                  %}
                  <div class="grid-item">
                    {% render 'card-product',
                      card_product: product,
                      show_vendor: section.settings.show_vendor,
                      show_stock: section.settings.show_stock,
                      show_rating: section.settings.show_rating,
                      show_sku: section.settings.show_sku,
                      show_add_to_cart: settings.products_show_add_to_cart,
                      mobile_two_columns: mobile_two_columns,
                      event_context: 'FeaturedCollectionSection',
                      lazyload: lazyload,
                      collection: section.settings.collection
                    %}
                  </div>
                {% else %}
                  {% for i in (1..section.settings.products_to_show) %}
                    <div class="grid-item">
                      {% render 'card-product',
                        show_vendor: section.settings.show_vendor,
                        show_stock: section.settings.show_stock,
                        show_rating: section.settings.show_rating,
                        show_sku: section.settings.show_sku,
                        show_add_to_cart: settings.products_show_add_to_cart,
                        mobile_two_columns: mobile_two_columns,
                        event_context: 'FeaturedCollectionSection'
                      %}
                    </div>
                  {% endfor %}
                {% endfor %}
              {% endfor %}

              {% if section.settings.show_collection == true
                and section.settings.collection_position == 'end'
              %}
                <div class="grid-item">
                  {% render 'card-collection-custom',
                    collection: section.settings.collection,
                    class: 'color-background-1',
                    image: section.settings.collection_image,
                    title: section.settings.collection_title,
                    description: section.settings.collection_description,
                    button_text: section.settings.collection_button_text,
                    settings: section.settings
                  %}
                </div>
              {% endif %}
            </div>

            {%- unless section.settings.stack_on_mobile -%}
              </div>
              </div>
            {%- endunless -%}

            {%- if section.settings.layout == 'slider' -%}
              {%- if section.settings.headings_alignment == 'center' -%}
                <carousel-navigation for="Carousel-{{ section.id }}">
                  <div class="visible-tablet-up">
                    <div class="pill-nav pill-nav-slider pill-nav-slider-inline-nav">
                      <button
                        class="pill-nav-item pill-nav-item-prev pill-nav-item-disabled focus-none js-prev"
                        aria-disabled="true"
                      >
                        <span class="visually-hidden">
                          {{- 'sections.slideshow.previous_slide' | t -}}
                        </span>
                        {% render 'angle', direction: 'left' %}
                      </button>
                      <button
                        class="pill-nav-item pill-nav-item-next js-next"
                        aria-disabled="false"
                      >
                        <span class="visually-hidden">
                          {{- 'sections.slideshow.next_slide' | t -}}
                        </span>
                        {% render 'angle', direction: 'right' %}
                      </button>
                    </div>
                  </div>
                </carousel-navigation>
              {%- endif -%}

              </carousel-slider>
            {%- endif -%}
          </div>
        </div>
      </div>
    </div>
  </div>
{%- endunless -%}
{% schema %}
  {
    "name": "t:sections.featured_collection.name",
    "tag": "section",
    "class": "section-featured-collection",
    "disabled_on": {
      "groups": ["header", "footer", "custom.overlays"]
    },
    "settings": [
      {
        "type": "header",
        "content": "t:sections.all.section_content.heading"
      },
      {
        "type": "inline_richtext",
        "id": "heading",
        "label": "t:sections.featured_collection.settings.heading.label",
        "default": "Featured collection"
      },
      {
        "type": "select",
        "id": "heading_size",
        "label": "t:sections.all.heading_size.label",
        "default": "md",
        "options": [
          {
            "value": "sm",
            "label": "t:sections.all.heading_size.options.label_sm"
          },
          {
            "value": "md",
            "label": "t:sections.all.heading_size.options.label_md"
          },
          {
            "value": "lg",
            "label": "t:sections.all.heading_size.options.label_lg"
          },
          {
            "value": "xl",
            "label": "t:sections.all.heading_size.options.label_xl"
          },
          {
            "value": "xxl",
            "label": "t:sections.all.heading_size.options.label_xxl"
          }
        ]
      },
      {
        "type": "inline_richtext",
        "id": "subheading",
        "label": "t:sections.featured_collection.settings.subheading.label"
      },
      {
        "type": "select",
        "id": "headings_alignment",
        "label": "t:sections.all.headings_alignment.label",
        "default": "left",
        "options": [
          {
            "value": "left",
            "label": "t:sections.all.headings_alignment.options.label_left"
          },
          {
            "value": "center",
            "label": "t:sections.all.headings_alignment.options.label_center"
          }
        ]
      },
      {
        "type": "collection",
        "id": "collection",
        "label": "t:sections.featured_collection.settings.collection.label"
      },
      {
        "type": "range",
        "id": "products_to_show",
        "min": 2,
        "max": 12,
        "step": 1,
        "default": 3,
        "label": "t:sections.featured_collection.settings.products_to_show.label"
      },
      {
        "type": "product_list",
        "id": "products",
        "label": "t:sections.featured_collection.settings.products.label",
        "info": "t:sections.featured_collection.settings.products.info"
      },
      {
        "type": "checkbox",
        "id": "show_vendor",
        "default": true,
        "label": "t:sections.all.product.show_vendor.label"
      },
      {
        "type": "checkbox",
        "id": "show_sku",
        "default": false,
        "label": "t:sections.all.product.show_sku.label"
      },
      {
        "type": "checkbox",
        "id": "show_stock",
        "default": true,
        "label": "t:sections.all.product.show_stock.label"
      },
      {
        "type": "checkbox",
        "id": "show_rating",
        "default": false,
        "label": "t:sections.all.product.show_rating.label",
        "info": "t:sections.all.product.show_rating.info"
      },
      {
        "type": "checkbox",
        "id": "show_collection",
        "default": true,
        "label": "t:sections.featured_collection.settings.show_collection.label",
        "info": "t:sections.featured_collection.settings.show_collection.info"
      },
      {
        "type": "header",
        "content": "t:sections.featured_collection.settings.header-collection-card.content",
        "info": "t:sections.featured_collection.settings.header-collection-card.info",
        "visible_if": "{{ section.settings.show_collection }}"
      },
      {
        "type": "select",
        "id": "collection_position",
        "label": "t:sections.featured_collection.settings.collection_position.label",
        "default": "end",
        "options": [
          {
            "value": "start",
            "label": "t:sections.featured_collection.settings.collection_position.options.label_start"
          },
          {
            "value": "end",
            "label": "t:sections.featured_collection.settings.collection_position.options.label_end"
          }
        ],
        "visible_if": "{{ section.settings.show_collection }}"
      },

      {
        "type": "image_picker",
        "id": "collection_image",
        "label": "t:sections.featured_collection.settings.collection_image.label",
        "info": "t:sections.featured_collection.settings.collection_image.info",
        "visible_if": "{{ section.settings.show_collection }}"
      },
      {
        "type": "select",
        "id": "collection_overlay_type",
        "label": "t:sections.all.overlay.overlay_type.label",
        "default": "gradient",
        "visible_if": "{{ section.settings.show_collection }}",
        "options": [
          {
            "value": "gradient",
            "label": "t:sections.all.overlay.overlay_type.options.gradient"
          },
          {
            "value": "color",
            "label": "t:sections.all.overlay.overlay_type.options.color"
          }
        ]
      },
      {
        "type": "color_background",
        "id": "collection_overlay_background",
        "label": "t:sections.all.gradient.overlay_background.label",
        "default": "linear-gradient(180deg, rgba(0, 0, 0, 0) 0%, rgba(0, 0, 0, 0.8) 100%)",
        "visible_if": "{{ section.settings.show_collection and section.settings.collection_overlay_type == 'gradient' }}"
      },
      {
        "type": "color",
        "id": "collection_overlay_color",
        "alpha": true,
        "label": "t:sections.all.overlay.overlay_color.label",
        "default": "#0000007F",
        "visible_if": "{{ section.settings.show_collection and section.settings.collection_overlay_type == 'color' }}"
      },
      {
        "type": "text",
        "id": "collection_title",
        "label": "t:sections.featured_collection.settings.collection_title.label",
        "visible_if": "{{ section.settings.show_collection }}"
      },
      {
        "type": "richtext",
        "id": "collection_description",
        "label": "t:sections.featured_collection.settings.collection_description.label",
        "visible_if": "{{ section.settings.show_collection }}"
      },
      {
        "type": "checkbox",
        "id": "hide_collection_description",
        "default": false,
        "label": "t:sections.featured_collection.settings.hide_collection_description.label",
        "visible_if": "{{ section.settings.show_collection }}"
      },
      {
        "type": "color",
        "id": "collection_text_color",
        "alpha": true,
        "default": "#ffffff",
        "label": "t:sections.featured_collection.settings.collection_text_color.label",
        "visible_if": "{{ section.settings.show_collection }}"
      },
      {
        "type": "text",
        "id": "collection_button_text",
        "label": "t:sections.featured_collection.settings.collection_button_text.label",
        "visible_if": "{{ section.settings.show_collection }}"
      },
      {
        "type": "color_scheme",
        "id": "collection_button_color_scheme",
        "label": "t:sections.featured_collection.settings.collection_button_color_scheme.label",
        "default": "background-1"
      },
      {
        "type": "select",
        "id": "collection_box_align",
        "label": "t:sections.all.box_align.label",
        "default": "bottom-left",
        "visible_if": "{{ section.settings.show_collection }}",
        "options": [
          {
            "value": "top-left",
            "label": "t:sections.all.box_align.options.label_tl"
          },
          {
            "value": "top-center",
            "label": "t:sections.all.box_align.options.label_tc"
          },
          {
            "value": "top-right",
            "label": "t:sections.all.box_align.options.label_tr"
          },
          {
            "value": "middle-left",
            "label": "t:sections.all.box_align.options.label_ml"
          },
          {
            "value": "middle-center",
            "label": "t:sections.all.box_align.options.label_mc"
          },
          {
            "value": "middle-right",
            "label": "t:sections.all.box_align.options.label_mr"
          },
          {
            "value": "bottom-left",
            "label": "t:sections.all.box_align.options.label_bl"
          },
          {
            "value": "bottom-center",
            "label": "t:sections.all.box_align.options.label_bc"
          },
          {
            "value": "bottom-right",
            "label": "t:sections.all.box_align.options.label_br"
          }
        ]
      },
      {
        "type": "header",
        "content": "t:sections.all.layout.heading"
      },
      {
        "type": "select",
        "id": "layout",
        "label": "t:sections.all.layout.layout.label",
        "default": "grid",
        "options": [
          {
            "value": "grid",
            "label": "t:sections.all.layout.layout.options.label_grid"
          },
          {
            "value": "slider",
            "label": "t:sections.all.layout.layout.options.label_slider"
          }
        ]
      },
      {
        "type": "select",
        "id": "columns",
        "label": "t:sections.all.layout.desktop_columns.label",
        "default": "4",
        "options": [
          {
            "value": "3",
            "label": "3"
          },
          {
            "value": "4",
            "label": "4"
          },
          {
            "value": "5",
            "label": "5"
          }
        ]
      },
      {
        "type": "checkbox",
        "id": "stack_on_mobile",
        "default": false,
        "label": "t:sections.all.layout.stack_on_mobile.label"
      },
      {
        "type": "select",
        "id": "mobile_columns",
        "label": "t:sections.all.layout.mobile_columns.label",
        "default": "1",
        "options": [
          {
            "value": "1",
            "label": "1"
          },
          {
            "value": "2",
            "label": "2"
          }
        ]
      },
      {
        "type": "header",
        "content": "t:sections.all.colors.heading"
      },
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "t:sections.all.colors.color_scheme.label",
        "default": "background-1"
      },
      {
        "type": "header",
        "content": "t:sections.all.section_spacing.heading"
      },
      {
        "type": "select",
        "id": "spacing_mode",
        "label": "t:sections.all.section_spacing.spacing_mode.label",
        "default": "desktop",
        "options": [
          {
            "value": "desktop",
            "label": "t:sections.all.section_spacing.spacing_mode.options.desktop"
          },
          {
            "value": "mobile",
            "label": "t:sections.all.section_spacing.spacing_mode.options.mobile"
          }
        ]
      },
      {
        "type": "range",
        "id": "spacing_top",
        "min": 0,
        "max": 150,
        "step": 2,
        "default": 60,
        "label": "t:sections.all.section_spacing.spacing_top.label",
        "visible_if": "{{ section.settings.spacing_mode == 'desktop' }}"
      },
      {
        "type": "range",
        "id": "spacing_bottom",
        "min": 0,
        "max": 150,
        "step": 2,
        "default": 60,
        "label": "t:sections.all.section_spacing.spacing_bottom.label",
        "visible_if": "{{ section.settings.spacing_mode == 'desktop' }}"
      },
      {
        "type": "range",
        "id": "spacing_top_mobile",
        "min": 0,
        "max": 150,
        "step": 2,
        "default": 48,
        "label": "t:sections.all.section_spacing.spacing_top.label",
        "visible_if": "{{ section.settings.spacing_mode == 'mobile' }}"
      },
      {
        "type": "range",
        "id": "spacing_bottom_mobile",
        "min": 0,
        "max": 150,
        "step": 2,
        "default": 48,
        "label": "t:sections.all.section_spacing.spacing_bottom.label",
        "visible_if": "{{ section.settings.spacing_mode == 'mobile' }}"
      },
      {
        "type": "header",
        "content": "t:sections.all.advanced.seo_heading.label"
      },
      {
        "type": "select",
        "id": "heading_tag",
        "label": "t:sections.all.advanced.heading_tag.label",
        "default": "h2",
        "options": [
          {
            "value": "h1",
            "label": "H1"
          },
          {
            "value": "h2",
            "label": "H2"
          },
          {
            "value": "h3",
            "label": "H3"
          }
        ]
      },
      {
        "type": "header",
        "content": "t:sections.all.advanced.visibility_heading.label"
      },
      {
        "type": "checkbox",
        "id": "show_on_desktop",
        "default": true,
        "label": "t:sections.all.advanced.show_on_desktop.label"
      },
      {
        "type": "checkbox",
        "id": "show_on_mobile",
        "default": true,
        "label": "t:sections.all.advanced.show_on_mobile.label"
      },
      {
        "type": "checkbox",
        "id": "hide_placeholders",
        "default": false,
        "label": "t:sections.featured_collection.settings.hide_placeholders.label",
        "info": "t:sections.all.advanced.hide_if_empty.info"
      }
    ],
    "presets": [
      {
        "name": "t:sections.featured_collection.presets.name",
        "category": "t:sections.all.preset_category.products"
      }
    ]
  }
{% endschema %}
